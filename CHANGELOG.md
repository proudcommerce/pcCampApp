# Changelog

## [3.0.4] - 2026-04-22

Admin-Edits der `event.json` wirken jetzt zur Laufzeit auch in Prod — ohne Rebuild. Bislang baken `build-cache-busting.cjs` die `{{EVENT_NAME}}`/`{{EVENT_SHORT_NAME}}`/`{{EVENT_HASHTAG}}`/`{{THEME_COLOR}}`/etc.-Platzhalter zur Build-Zeit fest in HTML und `manifest.json`, sodass Aenderungen im Admin-Tab „Event" im Frontend unsichtbar blieben. Der `src/assets/event-config-loader.js` ersetzt jetzt immer (Dev und Prod) die relevanten DOM-Felder aus `/content/event.json`: `<html lang>`, `<title>`, `<meta name="description|robots|theme-color|apple-mobile-web-app-title|msapplication-TileColor">`, `<img data-brand-logo alt>`, `<main h1>` (nur Startseite), `.copyright-left`/`.copyright-right`. PWA-`manifest.json` wird aus der runtime-Config als Blob-URL erzeugt und der `<link rel="manifest">` umgebogen — Add-to-Homescreen zieht damit ebenfalls ohne Rebuild die aktuelle Event-Konfiguration. Copyright/Hashtag weiter via `textContent` (XSS-Schutz fuer Admin-Content); ein `<a>`-Link im Copyright-Seed rendert dadurch als Literaltext und gehoert ins Template.

Zusaetzlich: Event-spezifische Style-Overrides ueber `content/assets/custom.css`. `src/assets/app.css` und `src/timetable/timetable.css` nutzen jetzt CSS-Custom-Properties (`--color-primary`, `--color-text`, `--color-border`, `--color-bg`, `--color-surface`, `--color-success`, `--color-danger`, u.a.) statt hartkodierter Hex-Werte. Alle oeffentlichen HTML-Einstiegspunkte (index, floorplan, food, sessionplan, sponsors, timetable) laden als letztes Stylesheet `content/assets/custom.css` — dort kann das Event per `:root{ --color-primary: #…; }` die Farbpalette umfaerben oder beliebige Regeln uebersteuern, ohne Code-Changes. Seed-Datei liegt in `seed/assets/custom.css` mit Token-Doku; `docker-entrypoint.sh` zieht sie auch in bestehenden Content-Volumes nach. Admin-UI bleibt bewusst ausgenommen.

## [3.0.3] - 2026-04-22

Sessionplan- und Timetable-UI entschlackt: Das Praefix „Raum" vor dem Raumnamen in der Sessionplan-Raum-Gruppierung entfaellt (`src/sessionplan/sessionplan.js`), der Summary-Text ist jetzt nur noch der Raumname. Zusaetzlich wird die Tages-Ueberschrift im Sessionplan und im Timetable automatisch ausgeblendet, wenn nur ein einziger Tag in den Daten vorhanden ist — damit verschwindet der redundante „Samstag"-/„Sonntag"-Header bei Single-Day-Events. Timetable wurde dazu in eine wiederverwendbare `renderDayContent()`-Funktion refaktoriert, die fuer Single-Day direkt in den Wrapper rendert und fuer Multi-Day weiterhin collapsible `<details>`-Gruppen erzeugt.

## [3.0.2] - 2026-04-22

Fix Admin-Panel hinter Apache-Reverse-Proxy in Prod (`https://example.com/admin` lief auf `http://…:5173/admin/` ins Leere). Ursache: nginx hat den automatischen Trailing-Slash-Redirect auf `/admin/` absolut mit seinem Container-internen Listen-Port gebaut und die `X-Forwarded-Proto`/`-Port`-Header von Apache ignoriert. Fix: `absolute_redirect off;` im Prod-Server-Block — nginx liefert jetzt nur noch relative `Location`-Header, und der Browser ergaenzt den korrekten Origin selbst.

Fix Event-Config-Speichern schlug mit `branding.logo invalid or unsafe` fehl, sobald der Default-Wert `./assets/logo.png` (aus `seed/event.json` bzw. `event.schema.json`) im Payload stand. Ursache: `isSafeUrl()` in `src/admin/schemas.php` akzeptierte als relative Pfade nur absolute (`/foo/bar`) oder ein einzelnes Segment (`foo.png`) — mehrsegmentige dot-relative Pfade wie `./assets/logo.png` fielen durch. Fix: Regex erweitert um `(\./)?segment(/segment)*` und `..`-Segmente explizit geblockt (kein Path-Traversal), Scheme-Whitelist und Kontrollzeichen-Block unveraendert.

## [3.0.1] - 2026-04-22

Security-Hardening-Rollup auf 3.0.0. Admin-API- und Voting-Payloads werden jetzt ueber einen schema-light Validator (`src/admin/schemas.php`) geprueft — erlaubt nur http/https/mailto/tel als URL-Schemes, blockt `javascript:`, `data:`, `vbscript:` und protokollrelative URLs, begrenzt Feldlaengen und lehnt `features.votingAdminKey` im Speichern-Flow aktiv ab. Voting-Rohdatei `votes.json` ist nicht mehr oeffentlich: nginx liefert sie mit 404 aus, Clients fragen Aggregate und den eigenen Vote-Status ueber den neuen Endpoint `src/votes/status.php` + `src/votes/vote-helpers.php` ab (keine userKeys anderer Nutzer im Response). Alle mutierenden Admin-Endpunkte (API, Upload, Voting-Status, Vote-Transfer) sind strikt Session + CSRF-Token (Header oder Form-Feld); Admin-Secrets sind nicht mehr im DOM. Session-Fixation behoben (`session_regenerate_id(true)` bei Login). Cookie-Flags `Secure`, `HttpOnly`, `SameSite=Lax` auf der Session gesetzt (Secure auch hinter TLS-terminierendem Reverse-Proxy via `X-Forwarded-Proto`). CORS-Wildcard aus der Admin-API entfernt — nur noch same-origin. GET-Parameter-Admin-Key-Fallback (`?key=...`) entfernt, damit der Key nicht mehr in Logs und Referer landet. `features.votingAdminKey`-Fallback in `event.json` entfernt. Admin-Backups landen unterhalb des Web-Roots in `content/.backups/`; nginx blockt Dotfiles und `.backup`-Dateien via Nested-Regex-Location. Security-Logging fuer fehlgeschlagene Admin-Logins, ungueltige API-Keys und CSRF-Mismatches. Zentrales Security-Header-Include (`nginx.security-headers.conf`) in allen Locations (X-Content-Type-Options, X-Frame-Options, X-XSS-Protection, Referrer-Policy). `.npmrc` mit `min-release-age=7d` als Supply-Chain-Guard gegen frisch veroeffentlichte boesartige npm-Pakete. Frontend: `food.js`, `sessionplan.js` und `event-config-loader.js` rendern Admin-Content jetzt ueber `textContent`/DOM-API statt `innerHTML`; iOS-Install-Banner wird ebenfalls per DOM konstruiert. Admin-Binary-Upload (`src/admin/upload.php`) mit MIME-Sniffing (SVG bewusst ausgeschlossen), 5 MB Cap, Extension-Whitelist und slug-sanitizedem Zielpfad fuer Logo/Floorplan/Sponsor-Logos. Container laufen durchgehend als non-root (PHP-FPM + nginx als `nginx`-User via `su-exec`). SQL/Key-File-Patterns in `.gitignore` ergaenzt.

## [3.0.0] - 2026-04-21

Architektur-Refactor: Saubere Trennung Code ↔ Content. Der komplette Build laeuft jetzt in einem Multi-Stage Dockerfile — lokales Node/npm ist nicht mehr erforderlich (`git clone && make prod-up`). Alle laufzeit-aenderbaren Daten (event.json, menu.json, news.json, sessions.json, timetable.json, food/*.json, sponsors.json, voting/*.json, Hash-Manifest, SW-Version) liegen jetzt im Host-Volume `./content/`, das beim ersten Container-Start aus `./seed/` geseedet wird. Code-Assets werden weiter vom Multi-Stage-Build ins Image gehasht (neues Manifest `assets-hashes.json`), Content-Rehashing uebernimmt `rehash.php` im Volume (Manifest `content/content-hashes.json`). Service Worker kombiniert jetzt eine Build-Version (`BUILD_VERSION`, beim Image-Build gesetzt) mit einer Content-Version (`content/sw-version.txt`, vom Rehash gebumpt), sodass sowohl Code-Deploys als auch Admin-Edits den Cache sauber invalidieren. `event.json` ist jetzt regulaer ueber die Admin-UI pflegbar (neuer Tab „Event", Raw-JSON-Editor). Zentraler PHP-Helper `content-paths.php` aufloest Content-Pfade konsistent in Dev und Prod. Neuer Admin-Binary-Upload (`admin/upload.php`, session-authentifiziert, MIME-gesniffed, max. 5 MB) fuer Logo, Floorplan und Sponsor-Logos — alle Dateien landen im Content-Volume und ueberleben Image-Rebuilds. Sponsor-Logos liegen jetzt strukturiert unter `sponsors/logos/`. Logo-Upload erzeugt zusaetzlich PWA-Icon-Varianten (favicon + 144/192/512) on-the-fly via PHP-GD; nginx liefert sie per `try_files /content$uri $uri` vor den Build-Defaults aus, sodass Favicon, Apple-Touch-Icon und Homescreen-Icon nach einem Upload automatisch mitziehen. Breaking: Alle URL-Pfade fuer dynamischen Content haben sich geaendert (z. B. `/menu.json` → `/content/menu.json`). Bestehende Installationen werden nicht migriert.

## [2.0.0] - 2026-04-11

Breaking Changes & Major Feature Release. Online Content-Verwaltung: Alle Event-Daten (Sessions, Timetable, News, Food, Sponsoren, Navigation) können jetzt direkt im Browser bearbeitet werden — ohne Build oder Deployment. Admin-Zugang über `/admin/?key=...` mit Session-basiertem Login. Voting-Admin wurde in das unified Admin-Panel integriert (`src/votes/admin.php` leitet weiter). Automatisches Backup bei jeder Änderung. Build-Prozess bewahrt Admin-Änderungen. Service Worker auf Network-first für JSON-Daten umgestellt. Runtime-Asset-Resolution via `cache-hashes.json` ersetzt Build-Time-Injection von gehashten JSON-Pfaden in JS/HTML. Production-Docker-Mount jetzt writable für Admin-Content-Management.

## [1.0.3] - 2026-04-10

Security-Hardening: Admin-Key aus event.json in Umgebungsvariable verschoben, Session-basierte Admin-Authentifizierung, CORS-Wildcard entfernt, Timing-safe Key-Vergleich, XSS-Schutz in Sponsor-Templates.

## [1.0.2] - 2025-10-29

Problem Votes-Administration behoben.

## [1.0.1] - 2025-10-28

Ersetze "docker-compose" durch "docker compose".

## [1.0.0] - 2025-10-27

Initialer Release auf Github nach erfolgreichem Live-Test auf dem DevOps Camp 2025.
